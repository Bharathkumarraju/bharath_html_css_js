The DOCTYPE tells the browser what rules to follow when showing the document to the user.

These rules are called the modes.

HTML5 DOCTYPE used in this book, that puts the browser into strict mode.

Another mode is called quirks mode.


HTML5 doctype looks like <!DOCTYPE html>
